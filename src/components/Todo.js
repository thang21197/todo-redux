import React from 'react';
import PropTypes from 'prop-types';

Todo.propTypes = {
    
};

function Todo(props) {
    console.log(props);
    return (
        <div>
            
        </div>
    );
}

export default Todo;